beom Commit Test
hyo commit test 
commit last test

--movie ���̺� ����
drop table tmovie;

create table tmovie( 
	mnum number primary key, --������ȣ
	mname varchar2(4000) not null, --������ �̸�
	mgenre varchar2(4000) not null, --��ȭ �帣
	mdir varchar2(4000) not null, --����
	mpro varchar2(4000) not null, --����
	mactor varchar2(4000) not null, --�ֿ�
	msup varchar2(4000), --����
	mage number not null, --���� ����
	mele number not null, --��ȭ ���
	mrdate date not null, --������
	midate date not null, --���ε���
	mrcount number not null, --��ȸ��
	mlike number not null, --��õ��
	murl varchar2(4000) not null, --���� ��ũ
	mrepo varchar2(4000) not null, --���� ����� ��ũ
	mwcon number not null -- ���� ȸ�� ���
);

drop sequence tmovie_seq;
create sequence tmovie_seq
  increment by 1
  start with 1
  minvalue 1
  maxvalue 100000
  nocache;

--user ���̺� ����
drop table tuser;

create table tuser( 
	unum number primary key, --������ȣ
	usid varchar2(4000) not null, --ȸ�� ���̵�
	uname varchar2(4000) not null, --ȸ�� �г���
	upw varchar2(4000) not null, --ȸ�� ��й�ȣ
	udate date not null,  --������
	ubirth date not null, --�������
	ugrade number not null, --ȸ�� ���
	uupstart date, --����� ȸ�� ����
	uupend date, --����� ȸ�� ��
	ugenre varchar2(4000) not null -- ��ȣ �帣
);

drop sequence tuser_seq;
create sequence tuser_seq
  increment by 1
  start with 1
  minvalue 1
  maxvalue 100000
  nocache;

--favorite ���̺� ����
drop table favorite;

create table favorite( 
	fnum number primary key, --������ȣ
	funum number not null, --ȸ�� ���� ��ȣ
	fmnum number not null -- ���ã�� ��� ��ȭ
);

drop sequence favorite_seq;
create sequence favorite_seq
  increment by 1
  start with 1
  minvalue 1
  maxvalue 100000
  nocache;
  
--genre ���̺� ����
drop table genre;

create table genre( 
	gnum number primary key, --������ȣ
	gname varchar2(4000) not null -- �帣��
);

drop sequence genre_seq;
create sequence genre_seq
  increment by 1
  start with 1
  minvalue 1
  maxvalue 100000
  nocache;

--mboard ���̺� ����
drop table mboard;

create table mboard( 
	bnum number primary key, --������ȣ
	btype number not null, --�Խñ� Ÿ��
	bmnum number not null, --��ȭ ������ȣ
	bunum number not null, --�ۼ��� ������ȣ
	bsubject varchar2(4000) not null, --���� ����
	bcon varchar2(4000) not null, --����(����) ����
	bref number not null, -- ��� �׷�
	brestep number not null, --��� ����
	brelevel number not null, --��� �ܰ�
	ddate date not null -- �ۼ���
);

drop sequence mboard_seq;
create sequence mboard_seq
  increment by 1
  start with 1
  minvalue 1
  maxvalue 100000
  nocache;


--tmovie 삽입예제
insert into tmovie values(tmovie_seq.nextval,'극한직업','코미디','이병헌','이병헌','류승룡,이하늬','진선규,이동휘',2,0,'2019-01-23',sysdate,0,0,1,'a','b','c','d');
insert into tmovie values(tmovie_seq.nextval,'뺑반','범죄,액션','한준희','한준희','공효진,류준렬,조정석','염정아,전혜진',2,5,'2019-01-30',sysdate,0,0,1,'a','b','c','d');
insert into tmovie values(tmovie_seq.nextval,'말모이','드라마','엄유나','엄유나','유해진,윤계상,김홍파','우현,김태훈',2,0,'2019-01-30',sysdate,0,0,1,'a','b','c','d');

--tuser 삽입예제
insert into tuser values(tuser_seq.nextval,'admin','admin','1234',sysdate,'2019-01-01',0,null,null,'a');
insert into tuser values(tuser_seq.nextval,'a1234','aaa','1234',sysdate,'2019-01-01',1,null,null,'코미디');

--genre 삽입예제
insert into genre values(genre_seq.nextval,'코미디');
insert into genre values(genre_seq.nextval,'액션');
insert into genre values(genre_seq.nextval,'범죄');
insert into genre values(genre_seq.nextval,'스릴러');
insert into genre values(genre_seq.nextval,'공포');
insert into genre values(genre_seq.nextval,'드라마');
insert into genre values(genre_seq.nextval,'멜로');

commit;

